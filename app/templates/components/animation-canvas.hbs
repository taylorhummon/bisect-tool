{{#if this.question}}
  <Question @question={{this.question}} />
{{/if}}
{{#each this.groupings as |grouping|}}
  <SmileyGrouping @grouping={{grouping}} />
{{/each}}
{{#if this.done}}
  <Done @done={{this.done}} />
{{/if}}

{{#if (eq this.grouping.faces.length 0)}}
  Done!
{{else}}
  {{#each this.grouping.faces as |face|}}
    <SmileyFace
      @face={{face}}
      @onSmileyClick={{this.smileyFaceClicked}}
    />
  {{/each}}
  <div class="number">
    {{this.grouping.integer}}
  </div>
{{/if}}

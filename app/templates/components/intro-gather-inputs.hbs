<div class="intro-gather-inputs">
  {{#unless (eq errors.length 0)}}
    <div class="errors">
      {{#each errors as |error|}}
        <div class="error">
          {{error}}
        </div>
      {{/each}}
    </div>
  {{/unless}}
  <div>
    {{smiley-face-happy}}
    <Input
      class="numerical-input"
      @value={{happyValueString}}
    />
  </div>
  <div>
    {{smiley-face-sad}}
    <Input
      class="numerical-input"
      @value={{sadValueString}}
    />
  </div>
  <button
    type="button"
    {{on "click" this.beginClicked}}
  >Begin</button>
</div>
